//Armar una función que reciba como parámetro un string y lo retorne invertido

const reverse =(string) =>{
    const array =[];

    for (let i = str.length; i >= 0; i--) {
        array.push(str[i]);
        
    }

    return array.join('');
}